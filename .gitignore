/bin/
/dist/
*.test
*.out

# The built CLI binary (`go build -o fovea ./cmd/fovea` in cli/), never
# committed. Anchored: an unanchored "fovea" pattern also matches the source
# directory cli/cmd/fovea/ and silently drops new files there.
/cli/fovea
