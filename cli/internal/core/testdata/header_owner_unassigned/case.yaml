describes: The header owner is "Unassigned" with odd case and whitespace; the check is normalised.
base: valid_complete
remove: []
expect:
  errors:
    - {rule: header_owner_unassigned, where: fovea.yaml}
